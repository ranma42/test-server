{
  "name": "test-server-sdk-sample",
  "version": "0.1.0",
  "private": true,
  "description": "Sample project for test-server-sdk using Jasmine",
  "scripts": {
    "build": "tsc",
    "test": "npm run build & jasmine --config=./jasmine.json",
    "test:record": "npm run build & jasmine --config=./jasmine.json -- --record"
  },
  "dependencies": {
    "test-server-sdk": "file:../"
  },
  "devDependencies": {
    "@types/jasmine": "^5.1.0",
    "@types/node": "^20.11.0",
    "cross-env": "^7.0.3",
    "jasmine": "^5.1.0",
    "typescript": "^5.3.0"
  }
}
